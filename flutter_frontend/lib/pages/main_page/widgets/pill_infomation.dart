import 'package:flutter/material.dart';

class PillInfomation extends StatelessWidget {
  const PillInfomation({super.key});

  @override
  Widget build(BuildContext context) {
    return const ListTile(
      title: Text('title'),
      subtitle: Text('subtitle'),
    );
  }
}
