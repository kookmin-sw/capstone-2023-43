query pill_infomation($itemSeq: String) {
  pb_pill_info(where: { item_seq: { _eq: $itemSeq } }) {
    name
    entp_name
    etc_otc_code
    class_name
    image_url
    use_method
    warning_message
    effect
  }
}
